import XCTest
@testable import jenkins_version_change_log

final class jenkins_version_change_logTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(jenkins_version_change_log().text, "Hello, World!")
    }
}
